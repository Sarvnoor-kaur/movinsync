package com.fleetbilling.util;

/**
 * Utility package placeholder — Phase 1.
 *
 * <p>Stateless helper classes and constants will live here.
 * Examples:
 * <ul>
 *   <li>DateUtils     — date range helpers for billing periods</li>
 *   <li>MoneyUtils    — BigDecimal rounding and paisa conversion helpers</li>
 *   <li>AppConstants  — application-wide constants</li>
 * </ul>
 *
 * <p>Utility classes should be final with a private constructor
 * (or static-only methods) and must contain no Spring beans.
 */
