package com.fleetbilling.repository;

/**
 * Repository layer placeholder — Phase 1.
 *
 * <p>All Spring Data JPA repositories will live here.
 * Each repository extends JpaRepository<Entity, IdType> and is used
 * exclusively by service classes — never by controllers.
 */
