package com.fleetbilling.service;

/**
 * Service layer placeholder — Phase 1.
 *
 * <p>All business logic will live in service classes within this package.
 * Services are annotated with @Service, injected into controllers via
 * constructor injection, and tested independently using mocks.
 *
 * <p>Rule: Controllers must NEVER contain business logic.
 * Business logic must NEVER go into repositories.
 */
