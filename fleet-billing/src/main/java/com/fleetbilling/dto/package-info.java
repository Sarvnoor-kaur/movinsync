package com.fleetbilling.dto;

/**
 * DTO layer placeholder — Phase 1.
 *
 * <p>Data Transfer Objects are simple Java records or classes used to carry
 * data between the client and the server. They decouple the API contract
 * from the internal database schema (entities).
 *
 * <p>Use Jakarta Bean Validation annotations (@NotNull, @Size, @Email, etc.)
 * on DTO fields — never on entity fields used as API input.
 */
