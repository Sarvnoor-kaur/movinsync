package com.fleetbilling.exception;

/**
 * Exception layer placeholder — Phase 1.
 *
 * <p>Custom exception classes and a global exception handler
 * (@RestControllerAdvice) will be defined here.
 *
 * <p>All exceptions thrown by services should be meaningful domain exceptions
 * (e.g., VehicleNotFoundException, DuplicateTripException) rather than
 * generic RuntimeExceptions. The global handler maps them to HTTP status codes.
 */
